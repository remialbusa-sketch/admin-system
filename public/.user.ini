; Local upload headroom for the import wizard (Excel/CSV workbooks).
; Covers FastCGI/CGI hosts; the built-in dev server reads php.ini instead.
upload_max_filesize = 64M
post_max_size = 68M
memory_limit = 512M
max_execution_time = 120
